import { Training } from "../pages";

const userRoutes = [
    {
        name: "Training",
        component: Training,
        exact: true,
        path: "/training"
    }
];

export default userRoutes;
